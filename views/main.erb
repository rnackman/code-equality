<!DOCTYPE html>
<html>
  <head>
    <title>=CodeEquality=</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="main.css">
  </head>

  <body>
  	<div id="main">
	  	<div class="row">
        <div id="header" class="col-md-10">=CodeEquality=</div>
      </div>
      <div id="leftBar" class="col-md-2">

      </div>
		  <div id="content" class="col-md-6">
		    <p class="title"><%= @header %></p>
        <a href="http://stats.grok.se/en/latest90/<%= @woman.name.gsub(" ", "_") %>" target="_blank"><%= @stat %></a>
        <p id="directive"><%= @directive %></p>

        <p id="bio"><%= @woman.bio %></p>

        <%= @twitter_intro %>
            <% @woman.tweets.each do |tweet| %>
              <%= tweet.html %>
            <% end %>
		  </div>

	  </div>

    <script>
    window.twttr = (function(d, s, id) {
    var js, fjs = d.getElementsByTagName(s)[0],
      t = window.twttr || {};
    if (d.getElementById(id)) return;
    js = d.createElement(s);
    js.id = id;
    js.src = "https://platform.twitter.com/widgets.js";
    fjs.parentNode.insertBefore(js, fjs);

    t._e = [];
    t.ready = function(f) {
      t._e.push(f);
    };

    return t;
    }(document, "script", "twitter-wjs"));
    </script>

  </body>
  <footer class="footer">
    <a href="/">SHOW ME ANOTHER</a>
  </footer>
</html>
