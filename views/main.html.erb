<h1><%= w.name %>

<!-- For Tweets about this individual
  tweets = []
  tweets = TweetGrabber.new.populate(w.name)
  tweets.each do |t|
    Tweet.create(body: t.text, individual_id: w.id)
  end -->

<!-- For WikiStats about this individual
WikiStats.new.scrape_stats(w.name).strip.delete("\n").gsub("_", " ")
